# Requirements  
- NodeJS
- MongoDB

# Configuration
1) npm install
2) Add .env-cmdrc file into the project directory.

Example:
```
{
  "dev": {
    "PORT": "3000"
  },
  "prod": {
    "PORT": "3000"
  },
  "test": {
    "PORT": "3000"
  }
}
```